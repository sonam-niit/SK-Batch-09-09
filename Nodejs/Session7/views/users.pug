h2 User's list 
table
    tr 
        th ID 
        th NAME 
        th EMAIL 
        th COUNTRY 
    each user in users 
        tr 
            td #{user.id}
            td #{user.name}
            td #{user.email}
            td #{user.country}